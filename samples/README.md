# Demo recording

Place the pre-recorded screen capture at `samples/weekly-reorder.mp4`. The committed
`weekly-reorder.json` is the stage-safe structured-output fallback when the recording,
ffmpeg, OpenAI API, or conference network is unavailable.
